<%*
const titleMatch = tp.file.title.match(/^(\d{4}-\d{2}-\d{2})日记$/);
const journalDate = titleMatch ? titleMatch[1] : tp.date.now("YYYY-MM-DD");

tR += `---
journal: 日记
journal-start-date: ${journalDate}
journal-end-date: ${journalDate}
---

1. `;
%>
