<%*
const titleMatch = tp.file.title.match(/^(\d{4})-(\d{1,2})周记$/);
const weekYear = titleMatch ? Number(titleMatch[1]) : moment().isoWeekYear();
const weekNumber = titleMatch ? Number(titleMatch[2]) : moment().isoWeek();
const weekStart = moment().isoWeekYear(weekYear).isoWeek(weekNumber).startOf("isoWeek");
const weekEnd = weekStart.clone().endOf("isoWeek");

tR += `---
journal: 周记
journal-start-date: ${weekStart.format("YYYY-MM-DD")}
journal-end-date: ${weekEnd.format("YYYY-MM-DD")}
---

## 一、本周工作

1. 待补充

## 二、下周计划

1. 待补充

## 三、本周工作时间线

### 周一（${weekStart.clone().add(0, "day").format("M月D日")}）

暂无工作记录。

### 周二（${weekStart.clone().add(1, "day").format("M月D日")}）

暂无工作记录。

### 周三（${weekStart.clone().add(2, "day").format("M月D日")}）

暂无工作记录。

### 周四（${weekStart.clone().add(3, "day").format("M月D日")}）

暂无工作记录。

### 周五（${weekStart.clone().add(4, "day").format("M月D日")}）

暂无工作记录。
`;
%>
